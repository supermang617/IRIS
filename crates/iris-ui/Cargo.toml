[package]
name = "iris-ui"
version = "0.1.0"
edition = "2024"

[dependencies]
iris-voice = { path = "../iris-voice" }
egui = "0.32"
eframe = { version = "0.32", default-features = false, features = ["default_fonts", "glow"] }
